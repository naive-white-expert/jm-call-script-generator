---
name: jm-call-script-generator
version: 5.0.0
description: JM外呼话术模板生成器。根据业务活动信息和模板文档，生成完整的AI外呼话术。触发场景：(1)用户提供活动信息+模板文档要求生成话术；(2)用户要求根据活动信息填充外呼模板；(3)用户提及"话术生成"、"外呼模板"、"填充模板"等关键词。支持Excel格式的外呼流程模板。⚠️ 必须先提取并确认活动信息，用户确认后才能生成话术。
github: https://github.com/openclaw/skills/tree/main/skills/jm-call-script-generator
emoji: 📞
---

# JM外呼话术生成器

## 核心功能

**本 Skill 的核心功能**：按照话术模板和活动信息，生成新话术

### 1.1 保持话术模板接口不变

- ❌ 不修改流程名称、节点名称、节点跳转逻辑
- ❌ 不修改用户回复选项、意向标签、短信触发逻辑
- ✅ 保持Excel结构（列数、合并单元格、Sheet顺序）

### 1.2 完成话术内容的重新生成

- ✅ 识别原模板中的完整句子
- ✅ 根据活动信息重新生成整句
- ✅ 保持语言风格一致（语气词、句式、称呼）
- ✅ 自动校验输出质量（语言通顺、内容正确、无模板残留）

---

## 使用方式

**用户提供**：
- 活动信息：{品牌活动信息文档}
- 模板文档：{外呼流程模板.xlsx}

**处理流程**：
```
1. 分析模板需求 → 识别模板中使用了哪些信息字段
    ↓
2. 提取活动信息 → 按标准格式提取信息
    ↓
3. 对比需求与提取 → 判断模板需要但缺失的信息
    ↓
4. 展示提取结果 → 清晰展示给用户查看（标注模板需求）
    ↓
5. 等待用户确认 → ⚠️ 必须等待，不能跳过
    ↓
6. 用户补充信息 → 仅补充模板用到的缺失信息
    ↓
7. 用户确认生成 → 明确回复后才能继续
    ↓
8. 识别模板格式 → 保证输出格式一致
    ↓
9. 分析模板内容 → 识别完整句子、提取语言风格
    ↓
10. 重新生成内容 → 完整句子替换
    ↓
11. 填充到模板 → 保持格式
    ↓
12. 自动校验 → 语言+内容+残留
```

**输出**：
- {活动名称}_话术.xlsx（包含：主流程、知识库、短信三个Sheet）

---

## 一、核心流程

### 步骤1: 分析模板需求并提取确认活动信息

**目标**：分析模板实际需求，提取相应的活动信息，确保用户确认后才能生成话术

**重要性**：🔴🔴🔴 最高优先级，必须执行

#### 1.1 分析模板需求

**目标**：识别模板中实际使用了哪些信息字段

**操作内容**：
1. 扫描主流程、知识库、短信三个Sheet的播报内容
2. 识别播报内容中的关键词：
   - 品牌相关：品牌名称、店铺名称
   - 活动相关：活动名称、活动时间
   - 产品相关：产品名称、产品特点
   - 权益相关：优惠券、红包、价格、赠品
3. 统计各信息字段的使用情况

**输出结果**：
```
模板需求分析结果：
✅ 品牌名称：在开场白、用户忙、知识库等节点使用 → 必须提供
✅ 店铺名称：仅在知识库"什么平台"问答使用 → 必须提供
⚠️ 活动时间：模板中未提及 → 可以不提供
✅ 产品名称：在多个节点和知识库使用 → 必须提供
✅ 优惠券信息：在开场白、用户忙等节点使用 → 必须提供
⚠️ 赠品信息：模板中未提及 → 可以不提供
```

#### 1.2 提取活动信息

**目标**：提取完整的活动信息，结构化整理

**提取框架（5类信息）**：

| 大类 | 必要性 | 字段名称 | 说明 |
|------|--------|----------|------|
| C1 品牌信息 | 按模板需求 | 品牌名称、店铺名称 | 品牌全称、店铺完整名称 |
| C2 活动信息 | 按模板需求 | 活动名称、活动时间 | 活动主题、有效期 |
| C3 产品信息 | 按模板需求 | 主要产品、产品特点 | 产品名称、规格、卖点 |
| C4 利益点信息 | 按模板需求 | 核心权益、价格优势、赠品 | 优惠券、红包、价格、赠品清单 |
| C5 场景类型 | ✅ 必填 | 有券/无券、宠物/非宠物、猫粮/犬粮 | 场景判断 |

**提取方法**：详见 [三.1 活动信息提取方法](#三详细方法)

#### 1.3 对比需求与提取结果

**目标**：判断哪些信息模板需要但缺失

**对比方法**：
将提取的活动信息与模板需求对比，判断：
- ✅ 模板需要且已提取 → 信息完整，可直接使用
- ⚠️ 模板未使用但已提取 → 信息可用，但非必需
- ❌ 模板需要但未提取 → 必须提示用户补充
- ⭕ 模板未使用且未提取 → 无需要求用户提供

**对比结果展示方式**：使用Markdown表格形式展示（详见步骤1.4）

#### 1.4 展示提取结果并等待用户确认

**目标**：清晰展示提取结果，等待用户确认

**展示方式**：使用Markdown表格形式，展示标准字段、模板使用内容、提取结果三列对比

**表格结构**：

| 标准字段 | 模板使用内容 | 提取结果 | 说明 |
|---------|-------------|---------|------|
| {字段名} | {模板中使用的具体位置和内容} | {提取结果} | {状态说明} |

**完整展示格式示例**：

```markdown
## 活动信息提取结果 - 请确认后再生成话术

### 【场景X】信息对比表

| 标准字段 | 模板使用内容 | 提取结果 | 说明 |
|---------|-------------|---------|------|
| 品牌名称 | 开场白："这边是XX的" | ✅/❌/⚠️ {提取结果} | 状态说明 |
| 店铺名称 | 知识库："邀请您参加的是【京东平台】【XX旗舰店】" | ✅/❌/⚠️ {提取结果} | 状态说明 |
| 活动名称 | 钩子："送您XX专属福利" | ✅/❌ {提取结果} | 状态说明 |
| 活动时间 | 模板未使用 | ✅/❌ {提取结果} | 模板未使用，可不提供 |
| 主要产品 | 用户忙："XX产品到手XX元" | ✅/❌ {提取结果} | 状态说明 |
| 产品特点 | 用户忙："XX规格/特点" | ✅/❌ {提取结果} | 状态说明 |
| 核心权益 | 开场白："给您XX券/红包" | ✅/❌/⚠️ {提取结果} | 状态说明 |
| 价格优势 | 用户忙："活动到手XX元" | ✅/❌ {提取结果} | 状态说明 |
| 赠品 | 模板未使用 | ✅/❌/⭕ {提取结果} | 模板未使用，可不提供 |
| 优惠券场景 | 开场白、用户忙等节点判断 | ✅ {判断结果} | 自动判断 |
| 行业类型 | 称呼判断（"毛孩子"/"家人"） | ✅ {判断结果} | 自动判断 |
| 宠物类型 | 称呼："给咱家毛孩子/狗狗囤点" | ✅/⭕ {判断结果} | 自动判断 |

**统计：** ✅ 完整X项 | ❌ 缺失X项（具体字段） | ⚠️ 待确认/不完整X项（具体字段）
```

**示例展示**：

```markdown
## 活动信息提取结果 - 请确认后再生成话术

### 【场景一】信息对比表

| 标准字段 | 模板使用内容 | 提取结果 | 说明 |
|---------|-------------|---------|------|
| 品牌名称 | 开场白："这边是XX的" | ❌ 未识别 | 必须补充 |
| 店铺名称 | 知识库："邀请您参加的是【京东平台】【XX旗舰店】" | ❌ 未识别 | 必须补充 |
| 活动名称 | 钩子："送您XX专属福利" | ✅ 10周年大促活动 | 可直接使用 |
| 活动时间 | 模板未使用 | ✅ 4.6-4.11 | 模板未使用，可不提供 |
| 主要产品 | 用户忙："XX产品到手XX元" | ✅ 厨房专用抽绳垃圾袋、10A抗菌纯棉T恤 | 可直接使用 |
| 产品特点 | 用户忙："XX规格/特点" | ✅ 抗菌材质 | 可直接使用 |
| 核心权益 | 开场白："给您XX券/红包" | ✅ 5元直减红包、200-30大促券 | 可直接使用 |
| 价格优势 | 用户忙："活动到手XX元" | ✅ 垃圾袋9.9元、T恤94元 | 可直接使用 |
| 赠品 | 模板未使用 | ⭕ 无赠品 | 模板未使用，可不提供 |
| 优惠券场景 | 开场白、用户忙等节点判断 | ✅ 有券场景 | 自动判断 |
| 行业类型 | 称呼判断（"毛孩子"/"家人"） | ✅ 日用品 | 自动判断 |

**统计：** ✅ 完整7项 | ❌ 缺失2项（品牌名称、店铺名称） | ⚠️ 可选2项

---

### 【场景二】信息对比表

| 标准字段 | 模板使用内容 | 提取结果 | 说明 |
|---------|-------------|---------|------|
| 品牌名称 | 开场白："这边是网易天成的" | ✅ 网易天成 | 可直接使用 |
| 店铺名称 | 知识库："邀请您参加的是【京东平台】【XX旗舰店】" | ⚠️ 未识别 | 可使用默认值 |
| 活动名称 | 钩子："送您网易天成XX专属福利" | ✅ 宠物专享福利 | 可直接使用 |
| 活动时间 | 模板未使用 | ❌ 未配置 | 不影响生成 |
| 主要产品 | 用户忙："XX产品券后XX元" | ✅ 全价冻干双拼猫粮3.0、兔肉美毛款 | 可直接使用 |
| 产品特点 | 知识库："XX规格、特点" | ✅ 1.8kg*4、含冻干 | 可直接使用 |
| 核心权益 | 开场白："给您无门槛8折券" | ✅ 无门槛8折券、299-45红包、599-90红包 | 可直接使用 |
| 价格优势 | 用户忙："XX产品券后XX元" | ✅ 猫粮3.0券后271.2元、兔肉美毛款263.2元 | 可直接使用 |
| 赠品 | 模板未使用 | ⭕ 无赠品 | 模板未使用，可不提供 |
| 优惠券场景 | 开场白、用户忙等节点判断 | ✅ 有券场景 | 自动判断 |
| 行业类型 | 称呼判断（"毛孩子"） | ✅ 宠物食品 | 自动判断 |
| 宠物类型 | 称呼："给咱家毛孩子囤点" | ✅ 猫粮 | 自动判断 |

**统计：** ✅ 完整9项 | ⚠️ 可选3项

---

## 📊 总览

| 场景 | 状态 | 需要操作 |
|------|------|----------|
| 场景一 | ❌ 缺失2项 | 必须补充：品牌名称、店铺名称 |
| 场景二 | ✅ 完整 | 可直接生成 |

---

## 📝 请选择下一步操作

**1️⃣ 补充缺失信息（推荐）**

需要补充的信息：
- 场景一：品牌名称、店铺名称

**2️⃣ 确认生成话术**

如果信息无误或您坚持在缺少信息的情况下生成，请回复'确认生成'

⚠️ 注意：缺少模板必需的信息可能导致话术质量下降

**3️⃣ 重新提供活动信息**

如果提取结果严重错误，请重新提供完整的活动信息
```

**图标说明**：
- ✅ 表示信息完整，可直接使用
- ❌ 表示必填信息缺失，必须补充
- ⚠️ 表示可选信息或待确认/不完整
- ⭕ 表示模板未使用，无需提供

**等待用户确认**：

```
请选择下一步操作:

1️⃣  补充缺失信息（推荐）
    请补充上述"必须补充的信息"，我将重新提取确认
    （注：模板未使用的信息可以不补充）

2️⃣  确认生成话术
    如果信息无误或您坚持在缺少信息的情况下生成，请回复'确认生成'
    ⚠️  注意：缺少模板必需的信息可能导致话术质量下降

3️⃣  重新提供活动信息
    如果提取结果严重错误，请重新提供完整的活动信息
```

**执行方法**：详见 [三.1 活动信息提取方法](#三详细方法)

#### 1.5 用户补充信息并再次确认

**目标**：用户补充缺失信息后，重新提取并确认

**操作流程**：
1. 用户补充缺失信息
2. 重新提取活动信息
3. 更新对比结果
4. 再次展示提取结果，等待用户确认
5. 用户确认后进入下一步骤

#### 1.6 用户确认生成

**目标**：用户明确确认后，开始生成话术

**确认规则**：
- 用户回复"确认生成" → 开始生成话术
- 用户坚持在缺少信息的情况下生成 → 提示风险后生成

**风险提示**：
```
⚠️ 风险提示：
您选择在缺少以下模板必需信息的情况下生成话术：
- {缺失信息1}（模板在XX节点使用了）
- {缺失信息2}（模板在XX节点使用了）

可能导致的问题：
- 品牌名称缺失 → 话术中使用占位符"XX品牌"
- 店铺名称缺失 → 可能影响品牌识别度
- 产品名称缺失 → 无法准确描述产品
- 优惠券信息缺失 → 权益描述不完整

✅ 以下信息虽然缺失，但模板未使用，不影响话术质量：
- 活动时间（模板未提及）
- 赠品信息（模板未提及）

建议：补充完整模板必需的信息后再生成，以确保话术质量。
```

---

### 步骤2: 识别并记录模板格式
**目标**：保证输出的格式一致

**操作内容**：
- 记录Excel结构（列数、行数、合并单元格位置）
- 记录Sheet顺序和名称
- 记录流程名称、节点名称、跳转关系
- 记录用户回复选项、意向标签、短信触发逻辑

**执行方法**：详见 [三.2 模板格式识别与保持](#三详细方法)

**如何运用知识库**：
- 参考 `REFERENCES.md` 第六章"Excel格式保持规范"

---

### 步骤3: 分析模板内容风格和结构
**目标**：识别需要重新生成的内容，保持语言风格一致

**操作内容**：
- 识别需要替换的完整句子（用户忙、重述、邀约到店等节点）
- 提取语言风格特征（语气词、句式结构、称呼方式）
- 分析句子结构（开场语+核心权益+价格说明+引导行动）

**执行方法**：详见 [三.3 模板内容分析](#三详细方法)

**如何运用知识库**：
- 参考 `REFERENCES.md` 第一章"错误1：部分替换导致不通顺"
- 参考 `REFERENCES.md` 第二章"替换规则详细示例"

---

### 步骤4: 重新生成内容
**目标**：根据活动信息和原模板结构，生成新话术

**操作内容**：
- 识别原模板中的完整句子
- 根据场景类型选择替换策略
- 保持语言风格一致
- **确保口语化表达和句子节奏**

**执行方法**：详见 [三.4 内容生成方法](#三详细方法)

**如何运用知识库**：
- 参考 `REFERENCES.md` 第二章"替换规则详细示例"
- 参考 `REFERENCES.md` 第七章"口语化表达规则"

---

### 步骤5: 填充到模板
**目标**：将生成的内容填充到模板，保持格式一致

**操作内容**：
- 将生成的话术填充到对应节点
- 更新知识库内容
- 更新短信模板
- 保持合并单元格、列宽等格式

**执行方法**：详见 [三.5 模板填充方法](#三详细方法)

**如何运用知识库**：
- 参考 `REFERENCES.md` 第六章"Excel格式保持规范"

---

### 步骤6: 校验输出质量
**目标**：语言通顺、内容正确、无模板残留、口语化自然

**操作内容**：
- 语言通顺性检查：检查重复词汇、不通顺表达
- 内容正确性检查：品牌、产品、权益是否正确
- 模板残留检查：原品牌、原活动名称是否残留
- 格式一致性检查：列数、行数、合并单元格是否一致
- **口语化校验：逐句朗读，确保像真人说话**

**执行方法**：详见 [三.6 质量校验方法](#三详细方法)

**如何运用知识库**：
- 参考 `REFERENCES.md` 第一章所有错误类型
- 参考 `REFERENCES.md` 第七章"口语化表达规则"

---

## 二、核心约束

### 2.1 流程约束

**⚠️ 步骤1（分析模板需求并提取确认活动信息）为强制步骤，必须执行**

#### 2.1.1 流程顺序约束

| 步骤 | 操作 | 是否可跳过 | 说明 |
|------|------|-----------|------|
| 步骤1.1 | 分析模板需求 | ❌ 不可跳过 | 必须执行 |
| 步骤1.2 | 提取活动信息 | ❌ 不可跳过 | 必须执行 |
| 步骤1.3 | 对比需求与提取 | ❌ 不可跳过 | 必须执行 |
| 步骤1.4 | 展示提取结果 | ❌ 不可跳过 | 必须展示给用户 |
| 步骤1.5 | 等待用户确认 | ❌ 不可跳过 | 必须等待用户回复 |
| 步骤1.6 | 用户补充信息 | ⚠️ 如有缺失则必须 | 缺失信息必须补充 |
| 步骤2-6 | 生成话术 | ❌ 必须在确认后 | 用户确认前禁止生成 |

#### 2.1.2 用户确认约束

**禁止行为**：
- ❌ 禁止跳过用户确认步骤
- ❌ 禁止用户未回复确认就自动生成
- ❌ 禁止不展示提取结果就直接填充模板

**允许行为**：
- ✅ 用户补充信息后重新提取并确认
- ✅ 用户明确回复"确认生成"后生成
- ✅ 用户坚持缺少信息生成时，提示风险后生成

---

### 2.2 信息确认约束

**核心原则：模板需求驱动**

#### 2.2.1 按模板需求判断必填信息

| 判断标准 | 说明 | 处理方式 |
|---------|------|----------|
| **模板使用了** | 模板播报内容中提及了该信息 | 🔴 必须提供并确认 |
| **模板未使用** | 模板播报内容中未提及该信息 | 🟢 可以不提供 |

**示例**：

| 信息类型 | 字段名称 | 判断依据 | 必要性 |
|---------|---------|----------|--------|
| 品牌信息 | 品牌名称 | 模板开场白："这边是XX的" | 🔴 模板需要，必填 |
| 品牌信息 | 店铺名称 | 知识库问答："什么平台" | 🔴 模板需要，必填 |
| 活动信息 | 活动时间 | 模板未提及时间 | 🟢 模板未使用，可选 |
| 产品信息 | 主要产品 | 多个节点提及产品 | 🔴 模板需要，必填 |
| 利益点信息 | 优惠券 | 开场白："给您XX券" | 🔴 模板需要，必填 |
| 利益点信息 | 赠品 | 模板未提及赠品 | 🟢 模板未使用，可选 |

#### 2.2.2 禁止要求用户提供模板未使用的信息

**禁止行为**：
- ❌ 禁止要求用户提供模板中完全没有提及的信息

**允许行为**：
- ✅ 如果模板未提及活动时间，可以不要求用户提供
- ✅ 如果模板未提及赠品信息，可以不要求用户提供

---

### 2.3 文件格式约束

**必须保持的格式项**：

| 格式项 | 约束规则 | 检查方法 |
|--------|----------|----------|
| Sheet结构 | 必须包含：主流程、知识库、短信三个Sheet | 列数、顺序严格不变 |
| 列数/行数 | 每个Sheet的列数和行数严格不变 | 对比原模板和输出文件 |
| 合并单元格 | 原模板所有合并区域必须保持 | 只修改左上角单元格值 |
| 流程名称 | 不修改任何流程名称 | 跳过流程名称列 |
| 节点名称 | 不修改任何节点名称 | 跳过节点名称列 |
| 节点跳转 | 不修改任何跳转逻辑 | 跳过跳转列 |
| 用户回复 | 不修改任何回复选项 | 跳过回复列 |
| 意向标签 | 不修改任何标签定义 | 跳过标签列 |
| 短信触发 | 不修改任何触发逻辑 | 跳过触发列 |

**输出文件命名**：`{活动名称}_话术.xlsx`

**输出文件路径**：
- 默认输出目录：`~/.openclaw/workspace/jm-call-script-output/`
- 输出文件完整路径：`~/.openclaw/workspace/jm-call-script-output/{活动名称}_话术.xlsx`
- 如果输出目录不存在，需要先创建

---

### 2.4 生成内容约束

**核心原则**：生成内容不允许编造，必须在活动信息范围内

| 约束项 | 说明 | 检查方法 |
|--------|------|----------|
| 品牌信息 | 只使用活动信息中提供的品牌名称 | 对比品牌字段 |
| 产品信息 | 只使用活动信息中提供的产品名称 | 对比产品字段 |
| 价格信息 | 只使用活动信息中提供的价格数据 | 对比价格字段 |
| 权益信息 | 只使用活动信息中提供的权益内容 | 对比权益字段 |
| 赠品信息 | 只使用活动信息中提供的赠品内容 | 对比赠品字段 |
| 活动名称 | 只使用活动信息中提供的活动名称 | 对比活动字段 |

**禁止行为**：
- ❌ 禁止编造品牌名称
- ❌ 禁止编造产品名称
- ❌ 禁止编造价格数据
- ❌ 禁止编造权益内容
- ❌ 禁止编造赠品信息
- ❌ 禁止添加活动信息中未提及的内容

**允许行为**：
- ✅ 允许调整表达方式（保持语言风格）
- ✅ 允许调整句子结构（保持句子完整）
- ✅ 允许调整引导方式（保持引导节奏）

---

## 三、详细方法

### 3.1 活动信息提取方法（对应步骤1）

#### 3.1.1 分析模板需求

**目标**：识别模板中实际使用了哪些信息字段

**分析方法**：
1. 加载模板Excel文件
2. 遍历主流程、知识库、短信三个Sheet
3. 对每个播报内容单元格，使用关键词匹配：
   - 品牌相关关键词：品牌名、店铺名、京东、天猫等
   - 活动相关关键词：活动时间、活动名称、限时、截止等
   - 产品相关关键词：产品名、规格、特点等
   - 权益相关关键词：优惠券、红包、价格、赠品等
4. 统计每个信息字段的使用频率和位置

**输出格式**：
```
模板需求分析结果：
{
  "品牌名称": {
    "使用频率": 8,
    "使用位置": ["开场白", "用户忙", "重述", "知识库-品牌问答", ...]
  },
  "店铺名称": {
    "使用频率": 2,
    "使用位置": ["知识库-平台问答", "知识库-渠道问答"]
  },
  "活动时间": {
    "使用频率": 0,
    "使用位置": []
  },
  ...
}
```

#### 3.1.2 提取活动信息

**提取框架**：

| 大类 | 字段名称 | 提取规则 |
|------|---------|----------|
| C1 品牌信息 | 品牌名称 | 从活动信息中提取品牌关键词 |
| C1 品牌信息 | 店铺名称 | 从活动信息中提取店铺完整名称 |
| C2 活动信息 | 活动名称 | 根据活动特征推断或提取 |
| C2 活动信息 | 活动时间 | 提取时间格式（X.X-X.X） |
| C3 产品信息 | 主要产品 | 提取产品名称列表 |
| C3 产品信息 | 产品特点 | 提取规格、卖点等 |
| C4 利益点信息 | 核心权益 | 提取优惠券、红包等 |
| C4 利益点信息 | 价格优势 | 提取价格、折扣信息 |
| C4 利益点信息 | 赠品 | 提取赠品清单 |
| C5 场景类型 | 优惠券场景 | 判断是否有券 |
| C5 场景类型 | 行业类型 | 判断行业 |
| C5 场景类型 | 宠物类型 | 判断猫粮/犬粮 |

**提取工具**：使用 `activity_info_extractor_v2.py`

#### 3.1.3 对比需求与提取结果

**对比逻辑**：
```python
def compare_template_needs(template_needs, extracted_info):
    """
    对比模板需求和提取结果
    
    Returns:
        {
            "完整信息": [],      # 模板需要且已提取
            "缺失信息": [],      # 模板需要但未提取
            "可选信息": [],      # 模板未使用
        }
    """
    result = {
        "完整信息": [],
        "缺失信息": [],
        "可选信息": []
    }
    
    for field, need_info in template_needs.items():
        if need_info["使用频率"] > 0:
            # 模板使用了该字段
            if extracted_info.get(field):
                result["完整信息"].append(field)
            else:
                result["缺失信息"].append(field)
        else:
            # 模板未使用该字段
            result["可选信息"].append(field)
    
    return result
```

#### 3.1.4 展示提取结果并等待确认

**展示工具**：使用 `activity_info_extractor_v2.py` 的 `format_for_confirmation` 方法

**确认机制**：
- 展示提取结果后，必须等待用户回复
- 提供3个选项：补充信息、确认生成、重新提供信息
- 用户明确回复后才能继续

---

### 3.2 模板格式识别与保持（对应步骤2）

#### 3.2.1 必须保持的格式项

| 格式项 | 说明 | 保持方法 |
|--------|------|----------|
| **合并单元格** | 原模板所有合并区域 | 只修改单元格值，不删除/新增合并区域 |
| **列数/行数** | 每个Sheet的列数和行数 | 严格不变 |
| **Sheet顺序** | Sheet的排列顺序 | 保持原顺序 |

#### 3.2.2 合并单元格处理

**关键点**：合并单元格区域内，只有左上角单元格有值

**处理方法**：
- 遍历合并单元格区域
- 只修改左上角单元格的值
- 其他单元格跳过

---

### 3.3 模板内容分析（对应步骤3）

#### 3.3.1 播报内容识别方法

**核心原则**：文档里所有播报内容都应该重新生成，根据内容类型采取不同处理策略

**识别流程**：
```
遍历所有播报内容列
    ↓
判断内容类型
    ↓
├── 内容为空 → 保持为空
├── 内容与活动无关 → 检查符合规范后原样保持
└── 内容与活动相关 → 识别完整句子，重新生成
```

**内容类型判断方法**：

| 内容类型 | 判断依据 | 处理策略 |
|----------|----------|----------|
| **空内容** | 播报内容单元格为空或只有空格 | 保持为空，不填充 |
| **活动无关内容** | 不包含品牌、产品、权益、价格、赠品等关键词 | 检查是否符合规范后原样保持 |
| **活动相关内容** | 包含品牌、产品、权益、价格、赠品等关键词 | 识别完整句子，重新生成 |

**活动无关内容的规范检查**：
- ✅ 不包含原模板的品牌名称
- ✅ 不包含原模板的活动名称
- ✅ 不包含原模板的产品名称
- ✅ 语言表达通顺
- ✅ 符合当前活动的场景逻辑

**不符合规范的无关内容处理**：
- 包含原模板残留 → 删除残留内容
- 语言不通顺 → 调整表达方式
- 不符合场景逻辑 → 重新组织句子

---

#### 3.3.2 活动相关内容的完整句子识别

**识别方法**：
1. 定位播报内容中的完整句子（以句号、问号、感叹号分隔）
2. 分析句子结构：开场语+核心权益+价格说明+赠品说明+引导行动
3. 标记需要替换的关键词：品牌、产品、权益、价格、赠品

**完整句子示例分析**：

| 原模板完整句子 | 句子结构分析 | 需替换关键词 |
|----------------|--------------|--------------|
| "我们给您京东账户赠送了一张50元专属优惠券，券后到手低至83元，还有猫条等9件好礼" | 开场语+权益+价格+赠品 | 京东账户、50元专属优惠券、83元、猫条、9件好礼 |
| "就是您账户里已经到账一张50元专属券，用券后到手低至83元" | 重述+权益+价格 | 50元专属券、83元 |

**识别重点**：
- 不是简单替换关键词，而是识别完整句子后整体重新生成
- 保持原句子的结构和节奏
- 保持原句子的语言风格（语气词、称呼方式）

---

#### 3.3.3 语言风格特征提取

**语言风格维度**：

| 维度 | 示例 | 说明 |
|------|------|------|
| 语气词 | "您好~"、"呢~"、"哦~" | 句首/句尾语气表达 |
| 句式结构 | "钩子+核心信息+引导行动" | 句子组成结构 |
| 称呼方式 | "您"（尊称）、"亲"（亲昵） | 对用户的称呼 |
| 表达风格 | "这边是..." vs "我是..." | 正式/口语/亲切 |

**提取方法**：
1. 从原模板播报内容中提取语气词
2. 分析句子的组成结构（开场语+核心信息+引导）
3. 识别称呼方式和对用户的称呼习惯
4. 判断表达风格（正式/口语/亲切）

**保持方法**：
- 在重新生成内容时，保持相同的语气词
- 保持相同的句子结构节奏
- 保持相同的称呼方式
- 保持相同的表达风格

---

### 3.4 内容生成方法（对应步骤4）

#### 3.4.1 核心原则

**完整句子替换**：识别原模板中的完整句子，根据活动信息重新生成整句，保持句子结构完整

**替换优先级**：长词组优先（避免多次替换导致不通顺）

#### 3.4.2 场景类型处理策略

| 场景类型 | 处理策略 |
|----------|----------|
| 有专属券场景 | 保留"券"的表达，替换券的类型和金额 |
| 无专属券场景 | 删除"券"的表达，重新组织句子结构 |
| 非宠物行业 | 替换行业词汇（"毛孩子"→"家人"） |
| 犬粮产品 | 替换产品词汇（"猫粮"→"犬粮"，"毛孩子"→"咱家狗狗"） |

---

#### 3.4.3 口语化表达规则（重要）

**核心原则**：电话话术必须口语化，避免书面化堆砌

**1. 权益引入方式**：

| 表达类型 | ❌ 书面化 | ✅ 口语化 |
|---------|----------|----------|
| 优惠券 | "赠送了50元优惠券" | "送了一张50元的优惠券" |
| 红包 | "5元直减红包" | "一张5元直减红包" |
| 使用引导 | "XX产品红包后XX元" | "用这个红包，XX产品到手只要XX元" |
| 时间限制 | "有效期4.6-4.11" | "有效期到4月11号，这几天都能用" |
| 价格优势 | "低至83元" | "到手只要83块钱" |

**2. 连接词使用规则**：

| 连接类型 | 必须使用 | 示例 |
|---------|---------|------|
| 权益引入 | "送了一张/个" | "送了一张5元红包" |
| 使用引导 | "用这个XX" | "用这个红包，垃圾袋到手9块9" |
| 信息串联 | "还有"、"像"、"另外" | "还有10周年大促券" |
| 场景举例 | "像XX" | "像咱家T恤，任选3件94块" |

**3. 禁止使用的书面化表达**：

```
❌ "赠送了XX券" → ✅ "送了一张XX券"
❌ "XX产品XX元" → ✅ "用这个券，XX产品到手只要XX元"
❌ "有效期X.X-X.X" → ✅ "有效期到X月X号"
❌ "红包后XX元" → ✅ "到手只要XX块X"
❌ "权益A、权益B、权益C" → ✅ "权益A。还有权益B。像权益C..."
```

---

#### 3.4.4 句子节奏规则（重要）

**核心原则**：单一权益一句话，信息分块表达

**1. 信息分块原则**：

```
完整话术 = 权益1 + 使用引导 + 产品价格1 + [停顿]
         + 权益2 + 叠加说明 + 产品价格2 + [停顿]
         + 权益3 + 产品价格3 + [收尾]
```

**2. 错误示例（信息堆砌）**：

```
❌ "来电呢是我们给您京东账户赠送了5元红包和200减30大促券，
京东京造10周年大促，厨房抽绳垃圾袋红包后只要9.9元，
纯棉抗菌纯棉T恤3件红包后94元"
```

**问题**：
- 多个权益塞在一句话
- 没有停顿和转折
- 听众难以消化

**3. 正确示例（分块表达）**：

```
✅ "来电呢是给您账户送了一张5元直减红包，有效期到4月11号。
[停顿] 用这个红包，咱们厨房专用的特厚抽绳垃圾袋，3卷60只到手只要9块9。
[停顿] 还有10周年大促券200减30，跟红包一起用能减35。
[停顿] 像10A抗菌纯棉T恤，任选3件只要94块钱。
[收尾] 诚邀您查收下活动短信来享受优惠呀。"
```

**4. 信息递进结构**：

| 步骤 | 内容 | 作用 |
|------|------|------|
| 第1句 | 核心权益 | 抓住注意力 |
| 第2句 | 使用引导+产品价格 | 告诉怎么用 |
| 第3句 | 次要权益+叠加说明 | 增加吸引力 |
| 第4句 | 其他产品价格 | 扩大选择 |
| 第5句 | 行动号召 | 引导参与 |

**5. 停顿位置规则**：

```
✅ 在以下位置停顿：
- 权益说完后 → "送了一张5元红包，有效期到4月11号。[停顿]"
- 产品价格说完后 → "垃圾袋到手只要9块9。[停顿]"
- 权益转换时 → "还有10周年大促券...[停顿]"
- 举例产品前 → "像XX产品...[停顿]"

❌ 避免连续说：
- "送了XX券XX产品XX元还有XX券XX产品XX元"
```

---

### 3.5 模板填充方法（对应步骤5）

#### 3.5.1 填充流程

```
1. 加载原模板（load_workbook）
    ↓
2. 遍历主流程节点
    ↓
3. 填充生成的话术到播报内容列
    ↓
4. 更新知识库
    ↓
5. 更新短信模板
    ↓
6. 保存文件
```

#### 3.5.2 注意事项

- 合并单元格：只修改左上角单元格
- 列宽/行高：自动保持
- 节点跳转逻辑：不修改

---

### 3.6 质量校验方法（对应步骤6）

#### 3.6.1 必须校验项

| 规则 | 检查内容 | 严重程度 |
|------|----------|----------|
| 语言通顺性 | 重复词汇、不通顺表达 | 🔴 高 |
| 内容正确性 | 品牌、产品、权益是否正确 | 🔴 高 |
| 模板残留 | 原品牌、活动、产品是否残留 | 🔴 高 |
| 格式一致性 | 列数、行数、合并单元格 | 🔴 高 |
| **口语化自然度** | **逐句朗读，是否像真人说话** | 🔴 高 |

#### 3.6.2 校验规则详情

**语言通顺性检查**：
- 检查重复词汇（如"券后到手券后476元"）
- 检查不通顺表达（如"的京东，"、"一张，"）

**内容正确性检查**：
- 品牌是否正确
- 产品信息是否出现
- 权益信息是否出现

**模板残留检查**：
- 原品牌名称是否残留
- 原活动名称是否残留
- 原模板特有内容是否残留（如"正装宠物香氛"）

**格式一致性检查**：
- 列数是否一致
- 行数是否一致
- 合并单元格数量是否一致

---

#### 3.6.3 口语化校验方法（重要）

**核心方法**：逐句朗读法

```
对生成的每一句话：
1. 大声朗读一遍
2. 问自己："真人会这么说话吗？"
3. 判断：通过/不通过
4. 不通过 → 标记问题 → 重写
```

**校验维度**：

| 维度 | 校验方法 | 通过标准 |
|------|----------|----------|
| **真人说话** | 闭上眼想象自己是客服，这句话会自然说出口吗？ | ✅ "送了一张5元红包"<br>❌ "赠送了5元直减红包" |
| **停顿节奏** | 一口气能读完吗？是否塞太多信息？ | ✅ 一句一个信息点<br>❌ 一句塞4个信息 |
| **使用引导** | 只说了有什么？还是告诉怎么用？ | ✅ "用这个红包，XX产品..."<br>❌ "XX产品红包后XX元" |
| **连接词** | 信息间有过渡吗？ | ✅ "还有"、"像"连接<br>❌ 直接并列堆砌 |
| **口语化表达** | 数字/时间/量词是否口语化？ | ✅ "9块9"、"4月11号"<br>❌ "9.9元"、"4.6-4.11" |

**问题标记规范**：

| 问题类型 | 说明 | 重写方向 |
|---------|------|----------|
| [口语化不足] | 表达书面化 | "赠送了"→"送了" |
| [信息堆砌] | 一句信息过多 | 拆分成多句 |
| [缺少引导] | 未引导使用 | 加"用这个XX" |
| [缺少连接词] | 无过渡词 | 加"还有"、"像" |
| [书面化表达] | 数字/时间未口语化 | "9.9元"→"9块9" |
| [缺少量词] | 无量词 | "优惠券"→"一张优惠券" |

**校验流程**：

```
生成话术后
    ↓
逐句遍历播报内容
    ↓
对每句话执行5维度检查
    ↓
记录问题句子
    ↓
├── 全部通过 → 保留原句
└── 有不通过 → 标记问题类型 → 重写 → 再次校验
```

**重写示例**：

**原句**：
```
"赠送了5元直减红包，厨房抽绳垃圾袋红包后9.9元"
```

**校验结果**：
```
[口语化不足] "赠送了"应改为"送了"
[缺少引导] 未引导使用
[书面化表达] "9.9元"应改为"9块9"
```

**重写后**：
```
"送了一张5元直减红包，有效期到4月11号。用这个红包，咱们厨房专用的抽绳垃圾袋，3卷60只到手只要9块9。"
```

**自动化校验规则**：

```python
def 口语化校验(句子):
    问题列表 = []
    
    # 维度1：真人说话检查
    if "赠送了" in 句子:
        问题列表.append("[口语化不足] '赠送了'应改为'送了'")
    
    # 维度2：信息密度检查
    if 句子.count("元") > 2:
        问题列表.append("[信息堆砌] 一句话价格信息过多")
    
    # 维度3：使用引导检查
    if "红包后" in 句子 or "券后" in 句子:
        if "用这个" not in 句子 and "到手" not in 句子:
            问题列表.append("[缺少引导] 缺少使用引导词")
    
    # 维度4：连接词检查
    if "和" in 句子 and len(句子) > 30:  # 长句用"和"连接
        if "还有" not in 句子:
            问题列表.append("[缺少连接词] 建议用'还有'替换'和'")
    
    # 维度5：口语化检查
    import re
    if re.search(r'\d\.\d元', 句子):  # 匹配"9.9元"
        问题列表.append("[书面化表达] 数字应口语化，如'9块9'")
    
    return 问题列表
```

**人工复核机制**：

**触发条件**：
- 自动校验全部通过，但话术仍感觉不自然
- 用户反馈话术生硬

**复核方法**：
1. 打印完整话术
2. 大声朗读一遍
3. 标记读起来不顺的地方
4. 按照口语化规则重写

---

### 3.7 知识库运营方法

#### 3.7.1 知识库的作用

**核心作用**：存储关键词问答库，用于AI对话中的快速响应

**知识库结构**：
| 关键词 | 机器人回答 | 说明 |
|--------|------------|------|
| 品牌相关 | 品牌介绍、店铺信息 | 品牌基础信息 |
| 产品相关 | 产品特点、规格说明 | 产品详细信息 |
| 权益相关 | 权益内容、使用规则 | 核心权益说明 |
| 价格相关 | 价格信息、优惠规则 | 价格和优惠详情 |
| 赠品相关 | 赠品清单、赠品规则 | 赠品详细信息 |

---

#### 3.7.2 知识库更新流程

**更新时机**：
- 生成新话术时：自动更新知识库内容
- 发现新的问答需求时：手动补充知识库

**更新流程**：
```
1. 提取活动信息中的关键词
    ↓ 识别：品牌、产品、权益、价格、赠品
2. 生成对应的标准回答
    ↓ 保持语言风格一致
3. 填充到知识库Sheet
    ↓ 保持原有条目数量
4. 检查知识库完整性
    ↓ 确保覆盖所有关键信息
```

---

#### 3.7.3 知识库内容规范

**回答风格要求**：
- 保持与主流程一致的语气词和称呼方式
- 回答简洁清晰，避免过长
- 使用标准话术表达

**禁止行为**：
- ❌ 禁止编造知识库中未提及的信息
- ❌ 禁止添加与活动无关的内容
- ❌ 禁止修改知识库的关键词类型

**允许行为**：
- ✅ 允许调整回答的表达方式（保持语言风格）
- ✅ 允许补充活动相关的常见问答

---

#### 3.7.4 知识库完整性检查

**必须包含的内容**：
| 检查项 | 说明 | 检查方法 |
|--------|------|----------|
| 品牌问答 | 品牌名称、店铺信息 | 对比品牌字段 |
| 产品问答 | 产品名称、规格特点 | 对比产品字段 |
| 权益问答 | 权益内容、使用规则 | 对比权益字段 |
| 价格问答 | 价格信息、优惠规则 | 对比价格字段 |
| 赠品问答 | 赠品清单、赠品规则 | 对比赠品字段 |

---

## 四、自学习能力

### 4.1 自学习机制

**核心功能**：记录对话中的好的case和坏的case，持续优化知识库和生成规范

**学习来源**：
- 用户反馈：成功的话术案例（好的case）
- 错误报告：失败的话术案例（坏的case）
- 质量检查：校验发现的问题类型

---

### 4.2 好的Case记录方法

**定义**：生成的话术语言通顺、内容准确、用户满意的案例

**记录内容**：
| 记录项 | 说明 |
|--------|------|
| 场景类型 | 有券/无券、宠物/非宠物、猫粮/犬粮等 |
| 原模板句子 | 原模板中的完整句子 |
| 生成结果 | 最终生成的话术 |
| 用户反馈 | 用户满意度评价 |
| 适用节点 | 该案例适用的节点类型 |

**记录位置**：
- 更新到 `REFERENCES.md` 第二章"替换规则详细示例"
- 新增"经验案例"小节

---

### 4.3 坏的Case记录方法

**定义**：生成的话术语言不通、内容错误、模板残留的案例

**记录内容**：
| 记录项 | 说明 |
|--------|------|
| 错误类型 | 参考REFERENCES.md第一章的5种错误类型 |
| 原模板句子 | 原模板中的完整句子 |
| 错误结果 | 错误的生成结果 |
| 问题原因 | 为什么会产生这个错误 |
| 修复方法 | 如何修复这个问题 |
| 防止方法 | 如何防止类似问题再次发生 |

**记录位置**：
- 更新到 `REFERENCES.md` 第一章"常见错误类型"
- 新增错误案例到对应的错误类型下

---

### 4.4 知识库更新机制

**触发条件**：
- 发现新的问答需求（用户提问未被知识库覆盖）
- 发现更好的回答方式（用户反馈更好的表达）
- 发现错误的回答内容（知识库内容不准确）

**更新流程**：
```
1. 记录新的问答需求
    ↓ 识别：关键词、用户提问、期望回答
2. 生成标准回答
    ↓ 保持语言风格一致
3. 更新到知识库
    ↓ 添加到知识库Sheet
4. 更新到REFERENCES.md
    ↓ 记录到详细文档中
```

---

### 4.5 持续改进流程

**定期检查**：
- 每周检查：是否有新的好的case或坏的case
- 每月总结：分析常见错误类型，优化生成规范
- 每季度更新：更新REFERENCES.md，沉淀经验

**改进优先级**：
| 优先级 | 改进内容 | 说明 |
|--------|----------|------|
| 🔴 高 | 错误修复 | 立即修复坏的case |
| 🟡 中 | 经验沉淀 | 记录好的case到示例库 |
| 🟢 低 | 知识库补充 | 补充新的问答需求 |

---

## 📚 参考资料

> **详细文档**: [REFERENCES.md](./REFERENCES.md)
> 
> 包含历史演进、版本迭代、常见错误类型、替换规则示例、详细规则说明等完整内容

---

**版本**: v5.0  
**更新**: 2026-04-08  
**重要变更**: 
1. 新增活动信息提取与确认流程（步骤1）
2. 新增模板需求分析机制，按模板需求判断必填信息
3. 不要求用户提供模板未使用的信息